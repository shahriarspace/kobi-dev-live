---
import { translations } from "../i18n";

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "AI coding assistant for Bangladeshi developers. Inline completions, chat, agent mode. Pay in BDT via bKash. From ৳500/month.",
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta
      name="keywords"
      content="AI coding, GitHub Copilot alternative, Bangladesh, BDT, bKash, কোডিং, AI, বাংলাদেশ, VS Code extension"
    />
    <meta name="author" content="Kobi" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://kobidev.com/" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://kobidev.com/og-image.png" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:locale:alternate" content="bn_BD" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />

    <!-- Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />

    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "SoftwareApplication",
        "name": "Kobi",
        "applicationCategory": "DeveloperApplication",
        "operatingSystem": "Windows, macOS, Linux",
        "description": "AI coding assistant for Bangladeshi developers. Pay in BDT via bKash.",
        "offers": [
          {
            "@type": "Offer",
            "price": "0",
            "priceCurrency": "BDT",
            "name": "Free"
          },
          {
            "@type": "Offer",
            "price": "500",
            "priceCurrency": "BDT",
            "name": "Basic"
          },
          {
            "@type": "Offer",
            "price": "1200",
            "priceCurrency": "BDT",
            "name": "Pro"
          }
        ]
      }
    </script>

    <title>{title}</title>
  </head>
  <body class="bg-kobi-bg text-kobi-text antialiased">
    <slot />

    <!-- Scroll-triggered fade-in animation -->
    <script>
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("visible");
            }
          });
        },
        { threshold: 0.1 }
      );

      document.querySelectorAll(".fade-in").forEach((el) => {
        observer.observe(el);
      });
    </script>

    <!-- i18n language toggle script -->
    <script define:vars={{ translationsJson: JSON.stringify(translations) }}>
      (function () {
        const translations = JSON.parse(translationsJson);
        const STORAGE_KEY = "kobi-lang";

        function getLang() {
          return localStorage.getItem(STORAGE_KEY) || "en";
        }

        function setLang(lang) {
          localStorage.setItem(STORAGE_KEY, lang);
        }

        function applyTranslations(lang) {
          const t = translations[lang];
          if (!t) return;

          document.querySelectorAll("[data-i18n]").forEach((el) => {
            const key = el.getAttribute("data-i18n");
            if (key && t[key] !== undefined) {
              el.textContent = t[key];
            }
          });

          // Update html lang attribute
          document.documentElement.lang = lang === "bn" ? "bn" : "en";

          // Update toggle button labels — show the OTHER language as the label
          const labelText = lang === "bn" ? "EN" : "বাং";
          const labelEl = document.getElementById("lang-label");
          const labelMobileEl = document.getElementById("lang-label-mobile");
          if (labelEl) labelEl.textContent = labelText;
          if (labelMobileEl) labelMobileEl.textContent = labelText;
        }

        function toggleLang() {
          const current = getLang();
          const next = current === "en" ? "bn" : "en";
          setLang(next);
          applyTranslations(next);
        }

        // Apply saved language on page load
        const savedLang = getLang();
        if (savedLang !== "en") {
          applyTranslations(savedLang);
        }

        // Bind toggle buttons
        const toggleBtn = document.getElementById("lang-toggle");
        const toggleBtnMobile = document.getElementById("lang-toggle-mobile");
        if (toggleBtn) toggleBtn.addEventListener("click", toggleLang);
        if (toggleBtnMobile) toggleBtnMobile.addEventListener("click", toggleLang);

        // MutationObserver: re-apply translations when React adds new elements with data-i18n
        // (e.g., CodeAnimation re-renders "Kobi is suggesting..." on each animation cycle)
        const mutationObserver = new MutationObserver((mutations) => {
          const lang = getLang();
          if (lang === "en") return; // English is the default, no need to translate
          const t = translations[lang];
          if (!t) return;

          for (const mutation of mutations) {
            for (const node of mutation.addedNodes) {
              if (node.nodeType !== Node.ELEMENT_NODE) continue;
              const el = node;
              // Check the added node itself
              if (el.hasAttribute && el.hasAttribute("data-i18n")) {
                const key = el.getAttribute("data-i18n");
                if (key && t[key] !== undefined) {
                  el.textContent = t[key];
                }
              }
              // Check descendants
              if (el.querySelectorAll) {
                el.querySelectorAll("[data-i18n]").forEach((child) => {
                  const key = child.getAttribute("data-i18n");
                  if (key && t[key] !== undefined) {
                    child.textContent = t[key];
                  }
                });
              }
            }
          }
        });

        mutationObserver.observe(document.body, {
          childList: true,
          subtree: true,
        });
      })();
    </script>
  </body>
</html>
